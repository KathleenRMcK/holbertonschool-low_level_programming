/**
 *main - function checks for lowercase
 *
 *return: 0
 */
#include "holberton.h"
int _islower(int c);
int c

if(c => 90 || c = 65)
  {
    if(c => 122 || c = 97)
      {
	return(1);
      }
    return(0);
  }
