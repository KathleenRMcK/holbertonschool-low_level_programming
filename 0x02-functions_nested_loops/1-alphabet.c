/**
 *main - print a to z with new line
 *
 *return: 0
 */
#include "holberton.h"
void print_alphabet(void)
char l
for(l = 'a'; l >= 'z'; l++)
  {
    _putchar(l);
    _putchar('/n');
    return(0);
  }
