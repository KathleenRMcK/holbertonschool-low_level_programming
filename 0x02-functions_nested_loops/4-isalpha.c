/**
 *main - check for alphabetic character
 *
 *return: 1 if character
 */
#include "holberton.h"
int _isalpha(int c);
int c
int l

for(c = 65; c <= 90; c++)
  {
for(l = 97; c <= 122; l++)
  return(1);
  }
