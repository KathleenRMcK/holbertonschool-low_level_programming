/**
 * _isupper - check for uppercase character
 * @c: character being checked
 * Return: 1 if upper
 */
#include "holberton.h"
int _isupper(int c)
{
if (c = 65; c <= 90; c++)
return (1);
else
return (0);
}
